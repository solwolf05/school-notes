views:
  - type: table
    name: Physics
    filters:
      and:
        - subject == "Physics"
    groupBy:
      property: unit
      direction: ASC
    order:
      - file.name
    sort:
      - property: file.mtime
        direction: ASC
